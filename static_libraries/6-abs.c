#include "main.h"
/**
*  _abs - prototype to return absolute value
*
* @x: is the integer checked
*
* Return: is void, Always 0
*/
int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	return (-x);
}